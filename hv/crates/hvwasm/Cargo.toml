[package]
name = "hvwasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM bindings for hvcore (hand-rolled, no wasm-bindgen)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hvcore = { path = "../hvcore" }
